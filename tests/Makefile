# Makefile for the CBSA COBOL isolated-test harness.
# Everything is a thin wrapper around run.sh so there is a single source
# of truth for the build/run pipeline.

.PHONY: test clean

test:
	./run.sh

clean:
	rm -rf build
