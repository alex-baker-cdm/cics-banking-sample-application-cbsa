# buildjcl

These are Job Control Language (JCL) files used to compile the base application code.

The default COBOL compiler high-level qualifier is set to `IGY640` (Enterprise COBOL V6.4) in `DEFAULT.jcl`. Update this value if your installation uses a different HLQ.
